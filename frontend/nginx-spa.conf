server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /argos/ {
        alias /usr/share/nginx/html/argos/;
        try_files $uri $uri/ /argos/index.html;
    }

    location / {
        return 301 /argos/;
    }
}
